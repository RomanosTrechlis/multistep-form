export type UserData = {
    firstname: string,
    lastname: string,
    age: string
}

export type AddressData = {
    street: string,
    city: string,
    state: string,
    zip: string
}

export type AccountData = {
    email: string,
    password: string
}